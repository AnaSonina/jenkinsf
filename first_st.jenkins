#!groovy
// Check URL properties
properties([disableConcurrentBuilds()])

pipeline{
  agent{
    lable 'master'
    }
  options{
    buildDiscarder(logRotator(unmToKeepStr: '10', artifactNumToKeepStr: '10'))
    timestamps()
  }
  stages{
    stage("First step"){
      steps {
        sh 'ping mail.ru/'
        }
      }
    stage("Second step"){
      steps {
        sh 'ping mtkp.bmstu.ru/'
        }
      } 
    }
}
